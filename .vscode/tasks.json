{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build-server",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/src/server/Dockerfile",
				"context": "${workspaceFolder}/src/server",
				"pull": true
			},
			"node": {
				"package": "${workspaceFolder}/src/server/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run-server: release",
			"dependsOn": [
				"docker-build-server"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/src/server/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run-server: debug",
			"dependsOn": [
				"docker-build-server"
			],
			"dockerRun": {
				"ports": [{
					"containerPort": 9000,
					"hostPort": 9000
				}],
				"env": {
					"APPLICATION_ID": "",
					"APPLICATION_SECRET": "",
					"CLIENT_URL": "http://localhost:8000",
					"DEBUG": "*",
					"GITHUB_USER": "",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/src/server/package.json",
				"enableDebugging": true
			}
		}
	]
}